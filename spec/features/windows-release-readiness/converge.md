<!-- GENERATED by spec-flow converge. Append-only drift ledger. -->

# Converge ledger: feat-004

## run 1 - 2026-07-29

baseline: spec sha256:1c14f509685b, plan sha256:0029d6664160, tasks sha256:ab53a9ae6218

implemented:

- AC-1: `.github/workflows/ci.yml` runs the quality suite on Windows and macOS.
- AC-2: `e2e/windows-smoke.e2e.mjs` drives the real Tauri executable through `tauri-driver`.
- AC-3: `.github/workflows/ci.yml` runs npm and Cargo audits; the updated npm graph reports zero
  vulnerabilities.
- AC-4: version sources are synchronized at 0.3.0 and local MSI plus NSIS builds completed.
- AC-5: `.github/workflows/build.yml`, `src-tauri/tauri.windows-release.conf.json`, and
  `scripts/qa/sign-windows-artifact.ps1` define OIDC authentication, post-patch signing, and
  fail-closed Authenticode verification.
- AC-6: the tag workflow publishes Windows MSI, Windows NSIS, macOS DMG, and SHA-256 checksum
  assets only after both platform jobs succeed.
- AC-7: `README.md`, `docs/windows.md`, and `docs/releasing-windows.md` document supported Windows
  versions, requirements, installation, verification, lifecycle operations, and troubleshooting.
- AC-8 and AC-9 remain unresolved human sign-offs in `feature.md` and unchecked human tasks in
  `tasks.md`. They are not implementation drift and prevent the feature from being marked done.

The automated scope matches the canonical spec and plan. No unrequested release fallback, signing
secret, or public unsigned Windows path was added.

verdict: open 0 (missing 0, partial 0, contradicts 0, unrequested 0)
