---
board:
  mode: native
  provider: null
  link_pattern: null
requires_design: true
tie_break: design-first
default_depth: ga
tests: ["src/**/*.test.ts", "src-tauri/src/**/*.rs", "e2e/**/*.e2e.mjs", "scripts/qa/**/*.ps1"]
---

Each feature's `spec.md` is canonical. `product.md`, `dashboard.md`, `analyze-product.md`, and
`view.html` are generated. Refresh them with `node scripts/assemble.mjs`,
`node scripts/dashboard.mjs`, and `node scripts/view.mjs`.

`constitution.md` and `product-global.md` are main-branch-only shared inputs. Start a feature with
the spec-flow feature workflow.
